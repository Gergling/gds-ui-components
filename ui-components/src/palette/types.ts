import { SemanticColor } from "../theme/types";

export type DisplayColor = {
  color: string;
  label: string;
};

export type DisplaySemanticColor = {
  semanticColor: SemanticColor;
  label: string;
};

