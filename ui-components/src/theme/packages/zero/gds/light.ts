import { getThemeConfiguration } from "../../../utilities";

export const zeroGDSLight = getThemeConfiguration({
  colors: {
    primary: {
      main: '#A31800',
    },
    secondary: {
      main: '#00BEF5',
    },
    tertiary: {
      main: '#038B00',
    }
  }
});
